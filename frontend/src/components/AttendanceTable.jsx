export default function AttendanceTable({ records, admin, onEdit, onCheckOut }) { return <div className="table-wrap"><table><thead><tr><th>Employee</th><th>Date</th><th>Status</th><th>Check in</th><th>Check out</th>{(admin || onCheckOut) && <th />}</tr></thead><tbody>{records.length ? records.map((item) => <tr key={item._id}><td><strong>{item.employee?.name || '—'}</strong><small>{item.employee?.employeeId}</small></td><td>{new Date(item.date).toLocaleDateString()}</td><td><span className={`status ${item.status.toLowerCase()}`}>{item.status}</span></td><td>{item.checkIn || '—'}</td><td>{item.checkOut || '—'}</td>{(admin || onCheckOut) && <td>{admin && <button className="text-button" onClick={() => onEdit(item)}>Edit</button>}{onCheckOut && item.status === 'Present' && !item.checkOut && <button className="text-button" onClick={() => onCheckOut(item)}>Check out</button>}</td>}</tr>) : <tr><td colSpan={admin || onCheckOut ? 6 : 5} className="empty-cell">No attendance records yet.</td></tr>}</tbody></table></div>; }
